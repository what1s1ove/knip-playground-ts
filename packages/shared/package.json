{
	"name": "@kp/shared",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"main": "build/index.js",
	"types": "build/index.d.ts",
	"scripts": {
		"postinstall": "npm run build",
		"build:clean": "rm -rf build",
		"build": "npm run build:clean && tsc"
	}
}
